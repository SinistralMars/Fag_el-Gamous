<!--
This is the "PredictionResult.cshtml" file, which is a Razor view that displays the prediction result of the head direction of a burial.
 The view receives an ApiResponse model that contains the predicted value.

The view checks if the ApiResponse model and its PredictedValue property are not null, indicating that a prediction result is available.
 If so, it displays the prediction result within an informational alert. If no prediction result is available, it displays a warning 
 message in an alert.

After displaying the prediction result or the warning message, a "Go back to form" button is provided to navigate back to the form for 
submitting another set of burial data for prediction.
-->

@model ApiResponse

@{
    ViewData["Title"] = "Prediction Result";
}

<h2>Prediction Result</h2>

@if (Model != null && Model.PredictedValue != null)
{
    <div class="alert alert-info mt-3">
        <strong>Prediction:</strong> @Model.PredictedValue
    </div>
 }
else
{
    <div class="alert alert-warning mt-3">
        <strong>Warning:</strong> No prediction result available.
    </div>
}

<a class="btn btn-primary" asp-controller="Prediction" asp-action="Index">Go back to form</a>

